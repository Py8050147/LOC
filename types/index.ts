export interface Courses {
    id: number;
    title: string;
    description: string;
    image: string;
    videoFile: string;
}

export interface Notes {
    id: number;
    title: string;
    description: string;
}